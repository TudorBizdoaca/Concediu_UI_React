import styles from 'assets/jss/styles'

const employeeStyle = theme => {
  return {
    page: {
      padding: '40px',
      backgroundColor: '#ffffff',
      boxShadow: '0px 4px 4px rgba(0, 0, 0, 0.04)',
      borderRadius: '14px'
    }
  }
}

export default employeeStyle
